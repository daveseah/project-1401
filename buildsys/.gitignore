# 1401 Game Additional Ignore Rules

# 1401 - Also ignore Mimosa-managed configuration and components
.mimosa
.npmignore
/bower_components
/public
/node_modules

# 1401 - Also ignore Durandal-managed components
/assets/javascripts/vendor
/assets/stylesheets/vendor

# 1401 - Also ignore mygames folder, so users can maintain a separate repo
/assets/mygames

# 1401 - Also ignore large binary assets that change frequently
# because we'll use something other than git to manage them
# - placeholder -